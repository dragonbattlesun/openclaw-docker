---
name: chanlun-engineering-label-discipline
description: 缠论分析必须区分工具标签、人工结构候选、原文严格买卖点和当前执行买点; 候选可以报告, 但不能被包装成严格买点或交易仓位
metadata:
  type: feedback
---

# 缠论工程标签与原文买点输出纪律(2026-06-03 整理)

## 教训

用户反馈: “为什么没有 1B 和 2B”。根因不是结构里完全没有候选, 而是输出时把:

- `没有原文严格买点`
- `没有当前位置可执行买点`
- `没有 1B/2B 候选`

混成了一句话: “没有买点”。

这会造成两个反向错误:

1. 把工具或人工观察到的 `1B/2B/3B 候选` 直接说成严格买点, 导致误入场。
2. 因为未达到严格入场标准, 就把已经出现的 `候选低点 / 等待二买 / 等待三买` 省略掉, 导致用户无法跟踪后续确认。

后续必须按 `reference_chanlun_108_core_rules.md` 的层级输出。

## 三层标签

| 层级 | 标签 | 含义 | 能否当入场依据 |
|---|---|---|---|
| 1 | 工具标签 | 扫描器或脚本输出的 `1B/2B/3B` | 不能 |
| 2 | 人工结构候选 | 人工合笔后看到的 `1B候选 / 2B候选 / 3B候选 / 盘整背驰候选 / 小转大候选` | 不能自动入场, 只能继续校验 |
| 3 | 原文严格买点 | 满足原文定义的 `严格1B / 严格2B / 严格3B` | 可以按对应级别和风控执行 |

输出时必须同时给出:

```text
工具标签: ...
人工结构: ...
原文严格: 通过 / 不通过, 缺什么
当前执行: 可入场 / 等待 / 不入场 / 只做持仓T
失效位: ...
```

## 统一买点判定

### 1B

原文严格 `1B` 必须同时满足:

1. 本级别下跌趋势成立, 不是普通中枢震荡。
2. c 段创新低后出现同级别背驰或明确力度衰竭。
3. 反弹笔成立。

允许报告但不能直接交易的标签:

- `1B候选`: 有下跌衰竭、盘整背驰、底分型或反弹笔雏形, 但趋势背驰条件未全。
- `盘整背驰候选`: 仍在中枢震荡里, 只可作为中枢下沿短差或观察。
- `小转大候选`: 小级别底背驰可能引发大级别转折, 但必须等待更大级别三买确认。

禁止:

- 把中枢震荡低点直接叫本级别严格一买。
- 因为不能入场就说“没有 1B”, 应说“有 1B候选, 但不是原文严格1B”。

### 2B

原文严格 `2B` 必须同时满足:

1. 前面已有有效 `1B`。
2. 之后第一次次级别回调。
3. 回调低点不破 `1B` 低点。
4. 回调不破关键中枢下沿或结构防守位。

允许报告但不能直接交易的标签:

- `2B候选`: 有回踩不破雏形, 但前面 `1B` 尚未严格成立。
- `等待2B`: 已有候选低点, 但还没出现第二次回踩不破。
- `2B失败`: 回踩跌破 1B 低点或关键防守位。

禁止:

- 没有严格 1B 时直接确认严格 2B。
- 把“当前不可以买”写成“没有 2B 路径”。正确写法是“等待回踩不破后才有 2B候选”。

### 3B

原文严格 `3B` 必须同时满足:

1. 次级别走势向上离开本级别中枢 `ZG`。
2. 离开后第一次次级别回试。
3. 回试低点不跌破 `ZG`。

允许报告但不能直接交易的标签:

- `3B候选`: 已突破或正在回试, 但首次回试级别/不破 `ZG` 未确认。
- `等待3B`: 只突破未回踩。
- `3B失败`: 回试跌回中枢或跌破 `ZG`。

禁止:

- 只有突破没有回试就说 3B 成立。
- 后续多次回踩都标成本级别 3B; 第三类买点只看离开后的第一次有效回试。

## 中枢震荡的特殊口径

`reference_chanlun_108_core_rules.md` 明确: 中枢震荡没有本级别一买、二买、三买, 只存在更小级别买卖点。

因此中枢内必须这样写:

```text
本级别: 中枢震荡, 无原文严格 1B/2B/3B。
小级别: 可在中枢下沿看低吸/回补候选, 在上沿看卖点/做T候选。
当前价若在中枢中部: 无当前位置执行买点。
```

不能这样写:

```text
没有买点。
```

应改成:

```text
有/无候选分别说明; 当前无执行买点; 等 xx 位置触发。
```

## 输出模板

以后个股缠论分析必须包含这个表:

| 级别 | 工具/表象 | 人工结构 | 原文严格结论 | 当前执行 | 失效位 |
|---|---|---|---|---|---|
| 日线 | ... | ... | ... | ... | ... |
| 60m | ... | ... | ... | ... | ... |
| 30m | ... | ... | ... | ... | ... |
| 5m | ... | ... | ... | ... | ... |

买点段落必须用这些词:

- `原文严格1B已成立`
- `1B候选, 未严格成立`
- `等待2B`
- `2B候选`
- `等待3B`
- `3B候选`
- `中枢震荡, 无本级别严格买点`
- `当前位置无执行买点`

禁止使用模糊词:

- `有买点`
- `没买点`
- `可以买一点`
- `形态不错`

除非同时给出级别、类型、触发位、失效位。

## 仓位纪律

| 标签 | 仓位口径 |
|---|---|
| 原文严格 1B | 可按低级别试错仓, 必须给候选低点失效位 |
| 原文严格 2B | 可按确认加仓, 跌破 1B 低点失败 |
| 原文严格 3B | 可按趋势仓, 跌回中枢或跌破 `ZG` 失败 |
| 候选 / 等待 / 盘整背驰 | 不给正式入场仓位; 只给观察、持仓T或触发条件 |
| 中枢中部 | 不追、不低吸、不做无优势 T |

候选可以报告, 但不能被包装成 `A0 2-3%` 交易档。若用户明确讨论已有仓位做 T, 只能按持仓管理给上下沿和失效位, 不能把它升级成新开仓买点。

## 示例: 拓维信息复盘口径

错误表达:

```text
没有买点。
```

正确表达:

```text
30m 处于 29.94-30.86 中枢震荡。
29.47 是跌破中枢下沿后的反拉低点, 可标为 30m 盘整背驰/1B候选观察点, 但不是原文严格1B。
当前 30.2 在中枢中部, 无当前位置执行买点。
若后续回踩不破 29.47 并再转强, 才进入 2B候选校验。
若放量离开 30.86 后第一次回踩不回中枢, 才进入 3B候选/严格3B校验。
跌破 29.47, 1B候选失败。
```

## 关联

- `memory/reference_chanlun_108_core_rules.md`
- `memory/reference_chanlun_recursive_buy_sell_framework.md`
- `memory/feedback_chanlun_continuous_multiday_bars.md`
- PDF: `/Users/dragonbattlesun/Downloads/缠中说禅教你炒股票108课.1完美配图版.pdf`

## 核心原则

**候选要说清, 严格要校验, 执行要给位。**

不能把候选当买点, 也不能把“未达严格买点”简化成“什么都没有”。
